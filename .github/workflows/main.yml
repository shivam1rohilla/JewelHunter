name: Upload a Build Artifact

on: [push]

jobs:
  uploadArtifact:
    runs-on: ubuntu-latest
    steps:
      - name: Upload Game
        uses: actions/upload-artifact@v2.2.4

        with:
            # Artifact name
            name: Jewel Hunter
            # A file, directory or wildcard pattern that describes what to upload
            path: JewelHunter.jar 
